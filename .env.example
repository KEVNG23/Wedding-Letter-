# Password for the /admin RSVP dashboard.
# If unset, the dashboard falls back to "annie-dung-2027" — change this before
# you share the site with guests.
ADMIN_PASSWORD=doi-mat-khau-cua-ban

# Optional. When present, RSVPs are stored in Postgres instead of a local
# JSON file. Railway injects this automatically once you add a Postgres
# service to the project.
# DATABASE_URL=postgresql://user:password@host:5432/dbname
